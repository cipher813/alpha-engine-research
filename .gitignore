.env
config/universe.yaml
config/prompts/
# config/prompts.example/ IS tracked — it's the template directory that
# mirrors scoring.sample.yaml. Real prompts (gitignored above) override
# the examples at runtime via the agents/prompt_loader.py fallback.
config/scoring.yaml
*.db
.coverage
DOCS.md
alpha-engine-research/
.venv/
*.db-shm
*.db-wal
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/
dist/
build/
.pytest_cache/
.DS_Store
lambda/package/
lambda-main.zip
lambda-alerts.zip
constituents_cache.csv
run.log
*-design-*.md
*-design.md
.ipynb_checkpoints/
*.ipynb

# ── Proprietary source (prompts, scoring, orchestration) ──────────
# Agents (contain prompts)
# config.py is now a loader only — proprietary values are in alpha-engine-config (private repo)

CLAUDE.md

# Gitleaks config (symlink to shared config)
.gitleaks.toml

.gitleaksbaseline.json
