# Core
anthropic~=0.40
langgraph~=0.2
langchain-anthropic~=0.3
langsmith~=0.7

# Evals
agentevals~=0.0

# Data
yfinance~=0.2.54
requests-ratelimiter~=0.7
pandas~=2.2
numpy~=1.26
requests~=2.31
feedparser~=6.0.11
beautifulsoup4~=4.12
lxml~=5.2

# Config / storage
pyyaml~=6.0.1
boto3~=1.34
pydantic~=2.7
# alpha-engine-lib provides setup_logging + BasePreflight. The
# [arcticdb] extra pulls arcticdb (used by data/fetchers/price_fetcher.py);
# [flow_doctor] pulls flow-doctor so lambda/handler.py's setup_logging
# call can attach the FlowDoctorHandler when FLOW_DOCTOR_ENABLED=1.
# Private repo — pip install needs ALPHA_ENGINE_LIB_TOKEN:
#   CI: .github/workflows/ci.yml configures a git URL insteadOf rewrite.
#   Lambda: Dockerfile installs from vendor/alpha-engine-lib staged by
#           infrastructure/deploy.sh (sibling checkout in CI, local dev on laptop).
alpha-engine-lib[arcticdb,flow_doctor] @ git+https://github.com/cipher813/alpha-engine-lib@v0.1.2

# Scheduling / calendar
exchange-calendars~=4.5
pytz~=2024.1

# DB
# SQLite is stdlib; no extra package needed

# Dev / testing
pytest~=8.2
pytest-asyncio~=0.23
python-dotenv~=1.0.1

# SEC EDGAR data
edgartools~=2.0

# RAG — vector retrieval over SEC filings + earnings transcripts
psycopg2-binary~=2.9
voyageai~=0.3
